<?xml version="1.0" encoding="utf-8"?>
<templates>
  <template type="public" title="hb_convo_conversation_view_buttons" version_id="0" version_string="1.0.0 Alpha"><![CDATA[<xf:if is="$recipient.User">
	<div class="contentRow-figure">
		<xf:avatar user="$recipient.User" size="xs" />
	</div>
	<div class="contentRow-main">
		<xf:username user="$recipient.User" rich="true" />
		<xf:if is="{{$recipient.User.user_id == $conversation.user_id}}">
			<xf:fa icon="fa-crown" data-xf-init="tooltip" title="{{ phrase('conversation_starter')|for_attr }}" />
		</xf:if>
		<div class="contentRow-minor"><xf:usertitle user="$recipient.User" /></div>
		<xf:if is="{$recipient.hb_invited_by} AND {$recipient.user_id} != {$conversation.user_id}">
			<div class="contentRow-minor">Invited by {$recipient.InvitedBy.username}</div>
		</xf:if>
	</div>
	<xf:else />
	<div class="contentRow-figure">
		<xf:avatar user="{{ null }}" size="xs" />
	</div>
	<div class="contentRow-main">
		{{ phrase('unknown_member') }}
	</div>
</xf:if>]]></template>
</templates>
